__pycache__/
*.py[cod]
.env
.venv/
venv/
.DS_Store
node_modules/
npm-debug.log*
# Registry signing keys — `mcp-publisher login http` reads key.pem from here, and a
# committed private key is a published private key. Never relax this.
key.pem
*.pem
